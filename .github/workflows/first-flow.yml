name: Complete-workflow
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: run cmd
        run: echo "Hello new World!"
      - name: run multiple cmd
        run: |
          npm -v
          node -v  